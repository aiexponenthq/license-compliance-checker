# Template for the LCC Kubernetes Secret. Copy this file to secret.yaml, fill in
# the values, and apply it separately. Do not commit a filled-in secret.yaml;
# it is ignored by .gitignore. In production, prefer an external secret manager
# (External Secrets Operator, Sealed Secrets, or your cloud provider's store).
apiVersion: v1
kind: Secret
metadata:
  name: lcc-secrets
type: Opaque
stringData:
  API_TOKENS: |
    github: ""
    clearlydefined: ""
